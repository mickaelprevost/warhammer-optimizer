{% extends 'base.html.twig' %}

{% block body %}

<div style="display:flex; justify-content:center; margin-top:3rem; width:100%; height:220px;">
					<img style="width:400px; height:100%;" src="{{ asset('asset/images/logo.jpeg') }}" alt="logo"> 
				</div>

<h2 style="text-align:center; text-transform:uppercase;margin-bottom:3rem;"> select your class </h2>
     <div class="order-liste" style="width: 100%;
    height: 400px;
    display: flex;
    flex-wrap:wrap;
    justify-content: center;
    gap: 2rem;
    font-size: 20px;">
        {% for classe in classes %}
            {% if classe.name == "shaman" or classe.name =="black-orc" or classe.name =="choppa" or classe.name =="squig-herder" %}
           <a style="text-decoration:none;" href="{{ path('app_classe', { id : classe.id })}}"> <img style= "width:280px; height:445px; border-radius: 15px;" src="{{asset('asset/images/classe/' ~ classe.picture)}}"></a>
            {% endif %}
        {% endfor %}
    <div>
{% endblock %}